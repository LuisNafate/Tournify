/**
 * Configuración de entorno de producción
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.tournify.com/api', // URL del backend en producción
  tokenKey: 'token',
  userKey: 'user'
};
