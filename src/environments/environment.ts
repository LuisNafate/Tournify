/**
 * Configuración de entorno de desarrollo
 */
export const environment = {
  production: false,
  apiUrl: 'http://34.235.8.163', // URL del backend Ktor (sin /api, puerto 80 es predeterminado)
  tokenKey: 'tournify_token',
  userKey: 'tournify_user'
};
