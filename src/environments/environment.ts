/**
 * Configuración de entorno de desarrollo
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080', // URL del backend Ktor (sin /api)
  tokenKey: 'tournify_token',
  userKey: 'tournify_user'
};
